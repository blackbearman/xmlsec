version: '3'

services:
  xmlsec:
    build: .
    restart: "no"
    command: bash "build.sh"
    volumes:
      - .:/usr/src
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"